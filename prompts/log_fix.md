将这个Python模块中的日志打印代码统一修改为以下风格：使用`logger.info()`方法，日志消息采用双引号包裹的字符串格式，内容简洁描述操作或状态；同时通过`extra`参数传递一个字典，字典键为小写字母且与上下文变量名一致，值为对应变量。例如：`logger.info("operation completed", extra={"a": a, "b": b, "c": c})`。要求对模块内所有日志打印语句进行检查和调整，确保风格一致，并避免引入新的语法错误或功能问题。
